# Python
.venv/
venv/
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Secrets / environment
.env
.env.*
# keep the placeholder template tracked; the real .env stays ignored above
!.env.example
*.key
.streamlit/secrets.toml

# Local editor / assistant tooling (not part of the project)
.claude/

# OS
Thumbs.db
.DS_Store

# Scratch
scratch/
*.tmp

# Generated review sheet (regenerate with scripts/build_review_sheet.py)
review/

# Internal / local-only source material (public+genericize policy — see corpus/GENERICIZATION.md)
# The internal compliance checklist is NEVER committed; it lives locally only.
sources/compliance_checklist_2026.xlsx
# Other internal compliance material (e.g. the Dos & Don'ts list) — never committed.
sources/*Dos and Don*.xlsx
